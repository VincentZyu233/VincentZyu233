# 部署配置 - 复制此文件为 .env 并填写实际值
# .env 文件已被 .gitignore 忽略，不会提交到仓库

# 远程服务器配置 (必填)
DEPLOY_REMOTE_HOST=your.server.ip
DEPLOY_REMOTE_PORT=22
DEPLOY_REMOTE_USER=root
DEPLOY_REMOTE_PATH=/path/to/your/site

# 本地配置 (可选，有默认值)
# DEPLOY_LOCAL_DIST_DIR=docs\.vitepress\dist
# DEPLOY_ZIP_NAME=dist.zip
